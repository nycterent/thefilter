# The Filter

Newsletter automation bot for curated content delivery.

## Setup

1. Install dependencies: `pip install -e .`
2. Configure environment variables
3. Run: `python -m src.newsletter_bot`

## Testing

Run tests with: `pytest tests/`

## Docker

Build: `docker build -t thefilter .`